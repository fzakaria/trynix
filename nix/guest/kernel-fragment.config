# Appended to the vendored qemu-wasm config before olddefconfig. The
# fork's examples boot from an ext4 disk image, so their config never
# enabled initramfs loading; trynix boots from an initramfs and mounts
# the store over 9p, no block device at all.
CONFIG_BLK_DEV_INITRD=y
CONFIG_RD_GZIP=y
